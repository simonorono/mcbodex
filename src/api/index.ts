import pokedexApi from './pokedex'
import pokemonApi from './pokemon'
import typesAPI from './types'

export const { getPokedexList } = pokedexApi
export const { getAllPokemon } = pokemonApi
export const { getAllTypes } = typesAPI
